#ifndef LER_HPP
#define LER_HPP
#include "map.hpp"

using namespace std;

void read();
void casasInexploradas();
void lerComparacao(vector<vector<int>> mat, int *casasInexploradas, int count);

#endif